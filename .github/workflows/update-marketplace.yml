name: Update Codex Marketplace

on:
  release:
    types:
      - published
  workflow_dispatch:
    inputs:
      ref:
        description: Git ref to publish into the marketplace snapshot
        required: false
        default: main

jobs:
  update-marketplace:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      MARKETPLACE_REPO: seankoji-com/codex-marketplace
      MARKETPLACE_BRANCH_PREFIX: auto/update-agy-
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ github.event.release.tag_name || inputs.ref || github.ref_name }}

      - name: Verify marketplace token is configured
        env:
          MARKETPLACE_TOKEN: ${{ secrets.SEANKOJI_CODEX_MARKETPLACE_PUSH_TOKEN }}
        run: |
          if [ -z "$MARKETPLACE_TOKEN" ]; then
            echo "SEANKOJI_CODEX_MARKETPLACE_PUSH_TOKEN is required to update $MARKETPLACE_REPO" >&2
            exit 1
          fi

      - name: Build marketplace snapshot
        run: |
          SNAPSHOT_DIR="$RUNNER_TEMP/marketplace-snapshot/agy"
          mkdir -p "$SNAPSHOT_DIR"
          for entry in \
            .codex-plugin \
            CHANGELOG.md \
            LICENSE \
            NOTICE \
            README.md \
            agents \
            assets \
            hooks \
            internal-skills \
            package.json \
            prompts \
            schemas \
            scripts \
            skills
          do
            if [ -e "$entry" ]; then
              rsync -a "$entry" "$SNAPSHOT_DIR/"
            fi
          done

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: seankoji-com/codex-marketplace
          path: marketplace
          token: ${{ secrets.SEANKOJI_CODEX_MARKETPLACE_PUSH_TOKEN }}
          fetch-depth: 0

      - name: Update marketplace snapshot
        run: |
          rm -rf marketplace/plugins/agy
          mkdir -p marketplace/plugins
          rsync -a --delete "$RUNNER_TEMP/marketplace-snapshot/agy/" marketplace/plugins/agy/

      - name: Create marketplace pull request
        id: create_pr
        env:
          GH_TOKEN: ${{ secrets.SEANKOJI_CODEX_MARKETPLACE_PUSH_TOKEN }}
          REF_NAME: ${{ github.event.release.tag_name || inputs.ref || github.ref_name }}
        run: |
          cd marketplace

          if git diff --quiet -- plugins/agy; then
            echo "Marketplace snapshot already matches $REF_NAME"
            echo "pr_number=" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          BRANCH_NAME="${MARKETPLACE_BRANCH_PREFIX}${REF_NAME//\//-}"
          git checkout -B "$BRANCH_NAME"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add plugins/agy
          git commit -m "Update agy plugin snapshot to $REF_NAME"
          git push --force-with-lease origin "$BRANCH_NAME"

          PR_NUMBER=$(gh pr list \
            --repo "$MARKETPLACE_REPO" \
            --head "$BRANCH_NAME" \
            --json number \
            --jq '.[0].number // empty')

          if [ -z "$PR_NUMBER" ]; then
            gh pr create \
              --repo "$MARKETPLACE_REPO" \
              --base main \
              --head "$BRANCH_NAME" \
              --title "Update agy plugin snapshot to $REF_NAME" \
              --body "Update \`plugins/agy\` in the seankoji-com Codex marketplace to \`$REF_NAME\` from \`seankoji-com/agy-plugin-codex\`."
            PR_NUMBER=$(gh pr list \
              --repo "$MARKETPLACE_REPO" \
              --head "$BRANCH_NAME" \
              --json number \
              --jq '.[0].number // empty')
          else
            echo "Marketplace PR already exists: #$PR_NUMBER"
          fi

          echo "pr_number=$PR_NUMBER" >> "$GITHUB_OUTPUT"

      - name: Merge marketplace pull request
        if: steps.create_pr.outputs.pr_number != ''
        env:
          GH_TOKEN: ${{ secrets.SENDBIRD_CODEX_MARKETPLACE_PUSH_TOKEN }}
          PR_NUMBER: ${{ steps.create_pr.outputs.pr_number }}
        run: |
          # Auto-snapshot PRs are deterministic from the source tag and need to
          # land without a human in the loop, otherwise releases ship to npm but
          # never reach the marketplace. We bypass branch protection here on
          # purpose; if the token does not have admin rights on the marketplace
          # repo, this step will fail loudly and the open PR remains for manual
          # merge — which is strictly better than silently leaving v1.x.0 stuck
          # in PR limbo.
          gh pr merge "$PR_NUMBER" \
            --repo "$MARKETPLACE_REPO" \
            --squash \
            --admin \
            --delete-branch
