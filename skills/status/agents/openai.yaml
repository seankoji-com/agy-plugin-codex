interface:
  display_name: "Antigravity Status"
  short_description: "Args: [job-id], --wait, --timeout-ms <ms>, --poll-interval-ms <ms>, --all. Inspect active or recent Antigravity jobs."
policy:
  allow_implicit_invocation: false
